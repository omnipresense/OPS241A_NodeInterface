echo off
echo Simplified launch of OmniPreSense module interaction server.
echo The preferred way to run the server is to run directly with "node index.js".
echo However, if there is only one OPS24X board attached, then the auto-guess of the serial port might work.
echo on
node index.js 
